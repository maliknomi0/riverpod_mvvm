import 'package:flutter/material.dart';

class LocalizationConfig {
  static const supportedLocales = [Locale('en', 'US'), Locale('ur', 'PK')];
  static const fallbackLocale = Locale('en', 'US');
  static const path = 'assets/translations';
}
